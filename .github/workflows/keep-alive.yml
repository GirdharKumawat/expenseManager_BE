name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET request to health check
        run: |
          curl -I https://expensemanager-be.onrender.com/api/auth/health/
